package space.vestiges.plugin1;

import com.google.gson.Gson;
import org.bukkit.plugin.java.JavaPlugin;

import java.io.File;
import java.io.FileWriter;
import java.io.IOException;
import java.util.UUID;

public class PlayerStatsStorage {

    // First time running, generate Folder for plugin

    // Need UUID, Base stats, Current stats, don't need defense, level

    //check if database exist, if not create one


}
